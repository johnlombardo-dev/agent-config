interface:
  display_name: "Scale Sol-Luna Goals"
  short_description: "Scale goals through implementation and delivery"
  default_prompt: "Use $scale-sol-luna-goals to coordinate Sol research, bounded Luna execution, verification, CI, and merge unless I explicitly restrict delivery."
policy:
  allow_implicit_invocation: false
