interface:
  display_name: "Scale Sol-Luna Goals"
  short_description: "Scale goals through implementation and review"
  default_prompt: "Use $scale-sol-luna-goals to coordinate Sol research, bounded Luna execution, and local plus hosted review convergence until the authorized goal is verified."
policy:
  allow_implicit_invocation: false
